node_modules
dist
.DS_Store
*.local
.vite

# Editor / OS
.idea
.vscode
*.swp
Thumbs.db

# Env
.env
.env.*
!.env.example

# Claude Code: per-machine settings and the temp files it writes beside them.
# .claude/settings.json (shared project config) stays tracked if you add one.
.claude/settings.local.json
.claude/*.tmp.*
